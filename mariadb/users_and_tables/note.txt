create user test@ip identified by 'pass'
grant select on test.* to test@ip; # attention pas de '' ici
use test;
create table test (a int not null);


[mysqld]
skip-networking=0
bind-address=0.0.0.0
systemctl restart mariadb
sudo iptables -u INPUT -p tcp --dport 3306 -j ACCEPT

